<%= form_with(model: error, local: true) do |form| %>
  <% if error.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(error.errors.count, "error") %> prohibited this error from being saved:</h2>

      <ul>
      <% error.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :error_title %>
    <%= form.text_field :error_title %>
  </div>

  <div class="field">
    <%= form.label :error_detail %>
    <%= form.text_area :error_detail %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
