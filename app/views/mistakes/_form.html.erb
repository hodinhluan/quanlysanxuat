<%= form_with(model: mistake, local: true) do |form| %>
  <% if mistake.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mistake.errors.count, "error") %> prohibited this mistake from being saved:</h2>

      <ul>
      <% mistake.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :mistake_title %>
    <%= form.text_field :mistake_title %>
  </div>

  <div class="field">
    <%= form.label :group_id %>
    <%= form.text_field :group_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
